import {FieldTypeEnum} from "../enums/field-type.enum";
import {MysqlColumnDefinition} from "../mysql/mysql-column-definition.interface";

export class MysqlFieldTypeMapper {
    map(fieldType: FieldTypeEnum): MysqlColumnDefinition {
        switch (fieldType) {
            case
        }
    }
}
